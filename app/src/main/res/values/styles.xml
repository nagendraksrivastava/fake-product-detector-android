<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorControlNormal">@color/text_color_primary</item>
    </style>

    <style name="ButtonStyle">
        <item name="android:textSize">@dimen/text_size_16sp</item>
        <item name="android:textColor">@color/button_text_color_selector</item>
        <item name="android:background">@drawable/button_selector_bg</item>
    </style>

    <style name="ButtonStyleWhite">
        <item name="android:textSize">@dimen/text_size_16sp</item>
        <item name="android:textColor">@color/button_text_color_selector_white</item>
        <item name="android:background">@drawable/button_selector_bg_white</item>
    </style>

    <style name="EditTextStyleSingleLine">
        <item name="android:textSize">@dimen/text_size_14sp</item>
        <item name="android:textColor">@color/text_color_primary</item>
        <item name="android:textColorHint">@color/grey_500</item>
        <item name="android:maxLines">1</item>
    </style>

    <style name="TextStyleMedium">
        <item name="android:textSize">@dimen/text_size_14sp</item>
        <item name="android:textColor">@color/text_color_primary</item>
    </style>

</resources>
